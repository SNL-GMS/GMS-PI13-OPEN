import { Tooltip } from '@blueprintjs/core';
import React from 'react';

export const STRAzimuthHeader: React.FunctionComponent<any> = props => (
  <Tooltip content="Source-to-Reciever Azimuth">S-R Azimuth (&#176;)</Tooltip>
);
