export const THOUSAND = 1000;

/** the max bar chart width - prevents the excessive wide bars when there are few channels */
export const MAX_BAR_CHART_WIDTH = 150;

/** the min bar chart width - prevents the excessive small bars when there are many channels */
export const MIN_BAR_CHART_WIDTH = 25;

/** the toolbar height in pixels - used to help adjust chart heights */
export const TOOLBAR_HEIGHT_PX = 30;
