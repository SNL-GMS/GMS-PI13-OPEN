// tslint:disable: no-empty-interface
import { ColumnApi as AgColumnApi } from 'ag-grid-community';

/** Wrapper interface class around ag-grid interface `ColumnApi` */
export interface ColumnApi extends AgColumnApi {}
