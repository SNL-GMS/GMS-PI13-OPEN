// tslint:disable: no-empty-interface
import { GridApi as AgGridApi } from 'ag-grid-community';

/** Wrapper interface class around ag-grid interface `GridApi` */
export interface TableApi extends AgGridApi {}
