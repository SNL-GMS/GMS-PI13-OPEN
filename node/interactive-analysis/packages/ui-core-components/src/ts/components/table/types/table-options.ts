// tslint:disable: no-empty-interface
import { GridOptions as AgGridOptions } from 'ag-grid-community';

/** Wrapper interface class around ag-grid interface `GridOptions` */
export interface TableOptions extends AgGridOptions {}
