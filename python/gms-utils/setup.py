from setuptools import setup, find_packages

# to install from the directory containing setup:
# pip install .

setup(
    name='gmsutils',
    version='0.1.0',
    description='GMS Python Utilities',
    packages=find_packages()
)
